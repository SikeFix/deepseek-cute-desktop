# DeepSeek Cute Desktop 1.4.1

本次重点解决 Windows 11 启动慢、双击后没有反馈，并加入 GitHub 联网自动更新。

## Windows 下载

- `DeepSeek-Cute-Windows-x64-Setup-1.4.1.exe`：Windows 10/11 x64 安装版
- `latest.yml` 与 `.blockmap`：应用自动更新和差分下载元数据
- `.sha256`：安装包完整性校验

## 主要优化

- 从便携单文件版改为 NSIS 安装版，避免每次启动重复解压约 800 MB
- 应用窗口立即出现，逐步显示服务检测、组件准备、启动和连接状态
- 修复服务已在线仍额外等待下一次 5 秒轮询的问题
- 首次启动阶段改为快速健康检查；本地开发验证界面切换由约 5.5 秒降至约 0.4 秒
- 后台服务写入诊断日志，托盘菜单可直接定位日志文件
- 自动从 GitHub Release 检查、下载、校验更新
- 标题栏显示检查、下载速度、百分比与更新完成状态
- 下载完成后可立即重启安装，也可退出应用时自动安装
- 仅保留简体中文与英文 Chromium 语言包，并移除非 x64 终端组件和调试符号

## macOS

本 Release 同时保留经过 M2 环境验证的 `DeepSeek-M2-1.4.0.dmg`。

## 签名说明

发布包是社区构建。Windows 版未使用商业代码签名证书，macOS 版采用 ad-hoc 签名且未公证；请从本仓库下载并核对 SHA256。
