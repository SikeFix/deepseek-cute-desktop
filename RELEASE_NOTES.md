# DeepSeek Cute Desktop 首个公开版

下载后直接使用：应用会自动启动随包附带的本地 DeepSeek 服务。

## 下载

- `DeepSeek-M2-1.4.0.dmg`：macOS 13.5+，Apple Silicon
- `DeepSeek-Cute-Windows-x64-1.0.0.exe`：Windows 10/11 x64，便携版
- 同名 `.sha256`：完整性校验文件

## 亮点

- 全局可爱暖色主题与沉浸式窗口
- macOS `.icns` 和 Windows 多尺寸 `.ico` 原生 App 图标
- 可拖动、可点击、会随任务状态变化的桌面宠物
- 本地服务自动启动、异常恢复、任务完成通知
- 关闭主窗口后可在后台继续运行

## 签名说明

两个发布包均为社区构建。macOS 版采用 ad-hoc 签名且未公证；Windows 版未使用商业代码签名证书。请从本仓库 Release 下载并核对 SHA256。
